## 1. PC Trust Endpoint Handlers

- [x] 1.1 Add trust handshake, apply, and confirm HTTP route handling to `_BootstrapHandler` in `https_bootstrap.py` (or extract a new `_InstantShareHandler` class) that dispatches to `TrustSession` / `TrustSessionRegistry` methods
- [x] 1.2 Implement `POST /api/instant-share/v1/trust/handshake` handler: parse `mobile_dh_public_key` and `mobile_nonce` from body, call `TrustSession.store_mobile_handshake` then `TrustSession.establish_session_key`, return `TrustSession.handshake_response` (pc_dh_public_key, pc_nonce, kdf_context); return 400 `INVALID_REQUEST` / `HANDSHAKE_REQUIRED` on validation errors
- [x] 1.3 Implement `POST /api/instant-share/v1/trust/apply` handler: require an established session key, call `TrustSession.decrypt_apply_request` on the envelope body, call `TrustSession.generate_pin` and `TrustSession.encrypted_pin_envelope`, return 202 with `apply_status: "accepted"` and the encrypted PIN envelope; return 400 `HANDSHAKE_REQUIRED` / `PAYLOAD_UNREADABLE` on errors
- [x] 1.4 Implement `POST /api/instant-share/v1/trust/confirm` handler: require an established session key and completed apply, call `TrustSession.decrypt_confirm_request` on the envelope body, call `TrustSession.mark_trusted` and `TrustSession.encrypted_trust_status`, return 200 with the encrypted trust-status envelope; return 400 `HANDSHAKE_REQUIRED` / `PAYLOAD_UNREADABLE` on errors
- [x] 1.5 Wire `TrustSessionRegistry` into `InstantShareBootstrapServer` (or the new server): create a `TrustSessionRegistry` alongside the existing `InstantShareSessionRegistry`, pass it to the HTTP handler so trust endpoints can create/lookup trust sessions by `X-Session-Id` header

## 2. PC Upload Endpoint Handlers

- [x] 2.1 Add `POST /api/instant-share/v1/transfer/text` route: read JSON body, extract `X-Session-Id` header, verify session exists and is trusted via `TrustSessionRegistry`, delegate to `TransferHandler.receive_text`, return 200 `{"state": "delivered"}`; return 403 `TRUST_REQUIRED` or 404 `SESSION_NOT_FOUND` on auth failures
- [x] 2.2 Add `POST /api/instant-share/v1/transfer/image` route: read binary body, extract `Content-Type` and `X-Instant-Share-Filename` headers, extract `X-Session-Id` header, verify session is trusted, delegate to `TransferHandler.receive_image`, return 200 `{"state": "delivered"}`; return 403 `TRUST_REQUIRED` or 404 `SESSION_NOT_FOUND` on auth failures
- [x] 2.3 Wire `TransferHandler` into the HTTP server: instantiate `TransferHandler` with `session_registry` and `delivery_service`, pass it to the handler class alongside `TrustSessionRegistry`

## 3. PC Session Lifecycle Updates

- [x] 3.1 Update `InstantShareSessionRegistry` (or `TrustSessionRegistry`) to allow trust session creation on bootstrap, so that a `TrustSession` is created when the bootstrap endpoint is called
- [x] 3.2 Update `InstantShareReceiverOrchestrator` to remove the outbound `InstantShareHttpClient`-driven trust flow (no longer calls `client.trust_handshake`, `client.trust_apply`, `client.trust_confirm`); the orchestrator now waits for inbound trust requests from iOS instead of driving them
- [x] 3.3 Update `InstantShareReceiverOrchestrator.receive_payload` to remove the outbound download calls (`client.download_text_payload`, `client.download_image_payload`, `client.report_delivery_result`); the PC now receives uploads inbound from iOS instead of pulling
- [x] 3.4 Add session state transitions for the new inbound flow: BOOTSTRAPPED → trust handshake received → NEGOTIATING → trust confirmed → TRANSFERRING (on upload) → DELIVERING → DONE
- [x] 3.5 Update the mini window (`mini_window.py`) to display the PIN generated by the PC in the `/trust/apply` handler and show trust/transfer progress for the inbound flow
- [x] 3.6 Wire the new trust/transfer endpoint handlers into `InstantShareRuntime.start()` so they start alongside the bootstrap server on port 9527

## 4. iOS Trust Client

- [x] 4.1 Create `InstantShareTrustClient.swift` in `AlbumTransporterKit/Services/` with methods: `handshake(host:port:mobileDHPublicKey:mobileNonce:)`, `apply(host:port:)`, `confirm(host:port:)`; each method makes an HTTP POST to the PC's bootstrap server
- [x] 4.2 Implement handshake: POST to `/api/instant-share/v1/trust/handshake` with `{mobile_dh_public_key, mobile_nonce}` (base64url-encoded), parse response `{pc_dh_public_key, pc_nonce, kdf_context}`, derive session key via HKDF-SHA256, store in `InstantShareTrustSessionManager`
- [x] 4.3 Implement apply: POST to `/api/instant-share/v1/trust/apply` with encrypted `{"action": "request_pin"}` envelope, decrypt the response envelope, extract and return the 6-digit PIN
- [x] 4.4 Implement confirm: POST to `/api/instant-share/v1/trust/confirm` with encrypted `{"action": "confirm", "pin_verified": true}` envelope, decrypt response envelope, verify `trust_status == "trusted"`
- [x] 4.5 Add error handling for each trust client method: map HTTP errors (400, 403, 404, 503) to Swift error types that map to the `ErrorCode` enum from `contracts.py`

## 5. iOS Upload Client

- [x] 5.1 Create `InstantShareUploadClient.swift` in `AlbumTransporterKit/Services/` with methods: `uploadText(host:port:sessionId:text:metadata:)`, `uploadImage(host:port:sessionId:imageData:contentType:filename:)`
- [x] 5.2 Implement text upload: POST JSON `{"text_utf8": "...", "metadata": {...}}` to `/api/instant-share/v1/transfer/text` with `X-Session-Id` header, parse `{"state": "delivered"}` response
- [x] 5.3 Implement image upload: POST binary body to `/api/instant-share/v1/transfer/image` with `Content-Type`, `X-Instant-Share-Filename`, and `X-Session-Id` headers, parse `{"state": "delivered"}` response

## 6. iOS Extension ViewModel Update

- [x] 6.1 Update `InstantShareExtensionViewModel` to replace the `InstantShareHTTPServer`-based flow with a sequential client flow: discover → bootstrap → handshake → apply → (display PIN + await user confirm) → confirm → upload → done
- [x] 6.2 Replace `InstantShareService` server-based trust flow with calls to `InstantShareTrustClient.handshake`, `.apply`, `.confirm` in sequence
- [x] 6.3 Replace `InstantShareService` server-based payload serving with calls to `InstantShareUploadClient.uploadText` or `.uploadImage`
- [x] 6.4 Update `InstantShareExtensionViewModel.send()` and `confirmPIN()` methods to use the new client calls instead of starting/stopping the HTTP server

## 7. Remove iOS HTTP Server

- [x] 7.1 Remove `InstantShareHTTPServer.swift` entirely (NWListener-based server and all 6 endpoint handlers)
- [x] 7.2 Remove server start/stop calls from `InstantShareService` (no more `httpsServer.start()`, `httpsServer.stop()`, `setSharedText`, `setSharedImage`)
- [x] 7.3 Remove `InstantShareHTTPServer` references from `InstantShareServices.swift` (protocol constants, endpoint paths that were server-side only)
- [x] 7.4 Clean up any `CheckedContinuation`-based long-poll logic in `InstantShareService` (`awaitUserConfirmation`, etc.) that was tied to the server-driven confirm flow

## 8. Update Protocol Contracts

- [x] 8.1 Update `contracts.py` `ErrorCode` enum to add `HANDSHAKE_REQUIRED`, `TRUST_REQUIRED`, `SESSION_NOT_FOUND` error codes if not already present; add `TRUST_HANDSHAKE_PATH`, `TRUST_APPLY_PATH`, `TRUST_CONFIRM_PATH`, `TRANSFER_TEXT_PATH`, `TRANSFER_IMAGE_PATH` constants
- [x] 8.2 Update `InstantShareServices.swift` protocol constants to define the new PC-hosted endpoint paths (`/trust/handshake`, `/trust/apply`, `/trust/confirm`, `/transfer/text`, `/transfer/image`)
- [x] 8.3 Update `Contracts.swift` / `InstantShareConnectionConfig` to use PC host/port for endpoint construction instead of mobile host/port, since iOS now calls PC (not the other way around)

## 9. End-to-End Integration

- [x] 9.1 Update `InstantShareRuntime.py` to start the extended HTTP server with all new endpoints (trust handshake, apply, confirm, transfer/text, transfer/image) on the same port as bootstrap
- [x] 9.2 Verify the bootstrap flow still works: iOS discovers PC via mDNS, sends bootstrap POST to port 9527, PC creates session and returns `accepted: true` (verified by code review — bootstrap handler logic preserved in `_InstantShareHandler`)
- [x] 9.3 Update telemetry spans and events in `orchestrator.py` to reflect the inbound trust/transfer flow (e.g., span names like `instant_share.trust.handshake.received` instead of `instant_share.trust.handshake.sent`)
- [x] 9.4 Add unit tests for the new PC-side trust handlers: handshake validation, apply decryption, confirm marking-trusted, error responses for missing/invalid sessions
- [x] 9.5 Add unit tests for the new PC-side upload handlers: text upload with valid trusted session, image upload with valid trusted session, 403 for untrusted session, 404 for unknown session
- [x] 9.6 Add unit tests / integration stubs for the iOS trust client and upload client methods